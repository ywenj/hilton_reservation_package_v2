# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Reservation {
  _id: ID!
  guestName: String!
  contactPhone: String!
  contactEmail: String
  expectedArrival: String!
  tableSize: Int!
  status: ReservationStatus!
}

enum ReservationStatus {
  Requested
  Confirmed
  Seated
  Completed
  Cancelled
}

type Query {
  reservations(date: String, status: String): [Reservation!]!
  reservation(id: String!): Reservation
}

type Mutation {
  createReservation(input: CreateReservationInput!): Reservation!
  updateReservation(id: String!, input: UpdateReservationInput!): Reservation!
  setReservationStatus(id: String!, status: String!): Reservation!
}

input CreateReservationInput {
  guestName: String!
  contactPhone: String!
  contactEmail: String
  expectedArrival: String!
  tableSize: Int!
}

input UpdateReservationInput {
  guestName: String
  contactPhone: String
  contactEmail: String
  expectedArrival: String
  tableSize: Int
}